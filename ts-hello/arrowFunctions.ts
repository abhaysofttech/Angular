
let log = function (message) {
    console.log(message);
}

let doLog = (message) => {
    console.log(message);
}

//make the do Log Function Shorter and cleaner

let doCleanLog = (message) => console.log(message); // called arrow function & in C# called labda expression

doCleanLog("text mesage");